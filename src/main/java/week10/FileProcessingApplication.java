package week10;

public class FileProcessingApplication {
    //todo
    public void processFiles(String inputFile, int targetMonth, String outputFile) {

//        Files.lines(filePath)
//                .map(line -> line.split(" "))
//                .forEach(lineArray -> new User(lineArray[0], lineArray[1], lineArray[2]));
    }
}
